# COACH.md — Strength Code OS · Continuity File

**Purpose of this file:** so this project survives anything — a deleted chat, a new device, a year from now. Keep it in the GitHub repo next to `index.html`. To resume coaching in any new conversation, upload **this file + your latest `strength-backup-*.json` + the current `index.html`** and say: *"Resume my Strength Code OS coaching from this file."* The new chat will have full context.

**Last updated:** 28 Jun 2026 — Phase 1 live, Phase 2 built, Phase 3–4 outlined. Fuel (nutrition) tab added. Program updated (ankle work + frontal-plane work). Hosting moved Netlify → GitHub Pages. **Animation engine added** (see §9): each exercise's "How it should look" is now a data-driven info-graphic showing muscle-firing sequence + tempo + phase cues. App = v11; SW cache = scos-v10. **Card/info split + smart charts** (see §10): Train card is minimal (Setup + Last + set tabs only); the target, quality cue, progression gate, and a per-exercise progress chart all live inside the ⓘ info page.

---

## 1. Who this is for
- Male, 38 · 180 cm · 84.5 kg · ~30–32% body fat at start. **Lacto-vegetarian, no eggs.** Based in Chennai, India.
- **Right shoulder:** multiple dislocations over ~15 years + stabilization surgery 18 months ago. This is the root cause of a right-side compensation, but it is **not** a single top-down chain (see DISCUSSION.md): treat it as the upper-right quadrant (shoulder → scapula → rear delt / external rotation → tricep) PLUS a semi-independent lower-right story where the **ankle** (local restriction) and **hip** squeeze the **right knee** (the victim).
- **Goal:** lean, athletic, durable body (Kohli-style) with usable strength and longevity. Return safely to **cricket and pickleball**. Lower body fat from ~32% toward ~15%. Movement quality is the primary metric; strength is the tool.
- **Training:** 3 sessions/week, 20–30 min each.
- **Equipment (fixed — never program around gear he doesn't own):** Inspire FT2 functional trainer (dual cables + Smith), adjustable bench, dumbbells, EZ curl bar, rope + handle attachments. **GMB mobility** subscription (use bodyweight/mobility freely). **No plyo box** — use the bench.
- **Baselines:** push-ups 4 · pull-ups 0 · right-leg balance 15 s · walking tolerance 15–20 min · deep squat needs ~25 mm heel elevation · loaded-ER shoulder pain ~5/10.
- **Nutrition start point:** protein ~40 g/day (self-limited). Target ~100 g/day to protect muscle in a deficit.

## 2. Non-negotiable principles (first principles)
1. **Movement quality > load > volume > intensity.** Quality gates everything.
2. **Foundation before progression** — never skip a floor.
3. **Equipment follows need, not the reverse.** Bench instead of a box; bodyweight/GMB counts fully.
4. **Educate every change.** Any add/remove/replace, or phase change, ships with a first-principles reason (logged in §6 and in the app's "Why your plan looks like this").
5. **The one progression rule:** add load only when **all target reps hit · form ≥ 7/10 · pain ≤ 3/10 · RIR ≥ 2 · compensation not worse.** Otherwise hold or regress.
6. **Progress is proven by tests, not by time elapsed** (see §4).
7. **Right-shoulder safety:** no loaded external rotation at 90° abduction (the apprehension position) until cleared by surgeon/physio. Keep OHP parked until the gates pass.

## 3. The phase model (the staircase)
- **Phase 1 — Restore & Stabilize (Floor 1):** rotator cuff, scapular control, single-leg stability, ankle mobility. No heavy load on an unstable base.
- **Phase 2 — Build & Integrate (Floor 2):** real load, unilateral strength, reintroduce vertical pulling + push-up/pull-up progressions, controlled rotation.
- **Phase 3 — Athletic Return (Floor 3):** power, rotational power, locomotion/agility, sport prep. *Authored from real test data on arrival.*
- **Phase 4 — Sport & Longevity (Floor 4+):** maintain + periodize for life; goals can evolve without losing the base.

## 4. Measurable tests + progression gates
Re-test every 2–4 weeks. All gates for a phase must pass to advance.

| Test | Goal it proves | Baseline | P1 gate | P2 gate | P3 gate |
|---|---|---|---|---|---|
| Right-leg balance (s) | CNS + right-side stability | 15 | 30 | 45 | 60 |
| Deep-squat heel lift (mm, lower=better) | Ankle/hip mobility | 25 | 15 | 5 | 0 |
| Push-ups (reps) | Press strength + shoulder | 4 | 8 | 12 | 15 |
| Walk tolerance (min) | Capacity / sport return | 20 | 25 | 35 | 45 |
| Pull-ups (reps) | Pull strength | 0 | 0 | 1 | 2 |
| Loaded-ER shoulder pain (/10, lower=better) | Right shoulder integrity | 5 | 2 | 1 | 0 |

## 5. Current programs
Full cues live in `index.html`. Rotation is **Lower → Upper → Athletic** (not weekday-bound; the next undone session waits).

**Phase 1 — Restore**
- *Lower:* **Ankle Rock** · CB Cable Squat · Cable RDL · Assisted Split Squat · Farmer Carry
- *Upper:* Wall Slide + Serratus Punch · Cable External Rotation (R) · Face Pull · Incline DB Press · Seated Cable Row
- *Athletic:* **Lateral Step-Down (R)** · Single-Leg RDL (R) · Low Step-Up · Pallof Press
- *Daily:* R/L balance (3:2) · Crawling · Deep-squat hold · Ankle dorsiflexion drill · Walking progression

**Phase 2 — Build** (unlocks when P1 gates pass)
- *Lower:* **Ankle Rock** · Goblet/Cable Squat · DB RDL · Bulgarian Split Squat · Loaded Step-Up
- *Upper:* Cable ER (R) · Push-up Progression · Seated Cable Row · Lat Pulldown · Face Pull
- *Athletic:* **Loaded Lateral Lunge** · Single-Leg RDL (R) · Cable Chop + Lift · Assisted Pull-up Negative

**Phase 3–4:** directions only in-app; finalized from real data on arrival.

**Fuel (nutrition) — runs every day, all phases.** Two rules, no calorie counting: (1) a protein anchor every meal (target ~100 g/day; veg anchors: whey scoop (×2/day), yogurt + granola, paneer, soy chunks, dal+curd, rajma, tofu, milk), (2) smaller portions of everything else. One instrument: a weekly weigh-in, read as a 3–4 week trend (down = deficit working; flat = trim non-protein portions). Gamified into the same XP/streak/level engine as training.

## 6. Change log (decisions + reasons)
1. **Added rotator-cuff & serratus work** (Wall Slide+Serratus, Cable ER, Face Pull), placed *before* pressing — the original plan ignored the shoulder, the actual root cause.
2. **Smith RDL → Cable RDL** — a fixed bar path hides right-side asymmetry; cable exposes and corrects it.
3. **Duplicate Smith RDL → Single-Leg RDL (R)** — removed redundancy, added a unilateral hinge that trains the balance test.
4. **Box squat → uses the bench** — no box to buy; equipment follows need.
5. **Parked Lat Pulldown & 1-Arm Press in Phase 1** — deferred until the cuff foundation passes its tests; pulldown reintroduced in Phase 2.
6. **Added Ankle Rock (Knee-to-Wall)** — the app had a heel-lift *test* (25→0 mm) but nothing *training* it, only a daily checkbox. Added one clean movement (drive the knee forward over a planted, heel-down foot) to both Lower days; it maps directly onto the heel-lift test so the number can move. (First version mashed two drills into one card and confused the user; simplified to a single movement, foot flat on the floor.)
7. **Box Squat → Lateral Step-Down (P1) / Loaded Lateral Lunge (P2)** — the program was 100% sagittal + anti-rotation, zero frontal-plane work, which is the plane cricket/pickleball live in and the plane the right knee fails in (caving inward). Box squat was also a duplicate bilateral squat. Lat pulldown deliberately kept parked (vertical pull belongs in P2; time budget is tight).
8. **Added the Fuel tab** — fat-loss (32→15%) was a primary goal with no app support. Two first-principles rules + a weekly-weight instrument, no calorie counting. The ~40 g/day protein was the real threat to muscle in a deficit; veg anchors fix it with no weighing.
9. **OHP stays parked; heavy leg press deferred** — OHP loads the shoulder near the apprehension position; heavy leg press drives the knee through a stiff ankle with the back supported, hiding the fault. Trap-bar deadlift, rows and DB pressing carry the load instead.

## 7. Tech architecture (how it all persists)
- **App:** one self-contained file `index.html` (vanilla JS PWA) + `sw.js` (offline cache + the thing that enables the install prompt). No build step.
- **Source of truth:** GitHub repo (files at root). Every commit is a restore point.
- **Hosting:** **GitHub Pages** (Settings → Pages → Deploy from a branch → `main`, folder `/ (root)`). Live at `https://<username>.github.io/<repo>/`. Redeploys on every push/upload. *(Previously Netlify.)*
- **Install on Android:** open the `github.io/<repo>/` URL in Chrome, interact for a few seconds, tap the in-app "Install to home screen" button — or Chrome ⋮ menu → "Add to Home screen". **`sw.js` must be in the repo root or the prompt never appears.**
- **User data:** browser `localStorage` key `scos_v1`, tied to the **site origin**. **Important:** the origin changed (Netlify → GitHub Pages), so data does **not** carry over automatically — migrate once via in-app **Export backup** (old site) → **Import backup** (new site). Within the same origin, data survives every deploy.
- **Backups:** in-app Progress → *Export backup* → `strength-backup-YYYY-MM-DD.json`. Monthly and before any major change.
- **State schema (v5):** `{rotationIndex, xp, history[], exHistory{}, activeDates[], bestStreak, drafts{}, habits{}, phase, assessments{}, setup{}, nutrition:{logs{}, weights[], xpDays[], bestFuelStreak}}`. `setup{}` maps exercise id → freeform machine-setup string (FT2 pulley/Smith #), set by the user, included in backups.

## 8. How to resume after a lost chat
1. Open a new conversation.
2. Upload: **this `COACH.md`**, **`DISCUSSION.md`**, your latest **`strength-backup-*.json`**, and the current **`index.html`** from the repo.
3. Say: *"Resume my Strength Code OS coaching from these files. Here's where I am."*
4. The assistant rebuilds full context from §1–§7 + DISCUSSION.md and continues under the same principles. Re-import the backup into the app if you switch devices.

*Optional convenience:* enabling memory / "search & reference past chats" in Claude's settings makes continuity automatic — but these files are the guarantee that doesn't depend on any single feature or chat.

## 9. Animation engine (firing-sequence info-graphics)
The per-exercise illustration is generated by a reusable engine, `exAnim(spec)`, from a data block in the `SPECS{}` object (both live in `index.html`). **Adding a future exercise = writing one short spec, not drawing.** A spec carries: `tempo{down,pause,up,hold}`, joint coordinates (`joints{name:{t:[x,y],b:[x,y]}}`), `bones[]` (joint chains drawn as limbs), optional `props`/`cable`/`equip`/`overlay`/`head`, `muscles[]` (each `{c,num,label,t,b,rx,ry,fire:{at,peak,off}}` for an ordered mover, or `{hold:true,...}` for a stabilizer/∞), and `cues[]` (phase text). The engine renders the figure animating top→bottom→top on the tempo cadence, muscle highlights brightening on their firing windows, a numbered firing-order legend, a tempo bar with a sweeping marker, direction arrows, and a footer.

**Honesty standard (non-negotiable, per the Judgment Protocol):** muscle *selection* (prime movers vs. stabilizers) is textbook EMG-grounded; the numbered *sequence* is an initiation/emphasis coaching cue, NOT a claim of literal millisecond EMG timing. Exercises with no real sequence are rendered honestly: carries & Pallof show holds-only ("WHAT HOLDS — no firing order"); external rotation shows a single "PRIME MOVER"; the ankle drill shows "TISSUE FREED (mobility, not firing)". We do NOT claim the animation is the "fastest" path — it is a form aid; progress comes from consistent practice, progressive load, and passing the test gates. All 23 current exercises have specs; the firing-selection table was audited before build (ask to see it again any time).


## 10. Card / info split + smart progress charts
Layout principle: **the Train card is for doing; the ⓘ info page is for understanding.** This replaced an earlier crowded card (rejected by the user as "messy, not minimalist").

**Train card (minimal):** name + ⓘ, one combo box with **Setup** (per-exercise FT2 pulley/Smith string, `S.setup[id]`, blank until set, in backups) and **Last** (auto top-load × clean reps · effort), then the set tabs. The "how did that feel?" effort rating appears under the tabs **only after all sets are logged**. Nothing else on the card.

**ⓘ info page (everything else, grouped):** firing-sequence animation → **Today's target** card (target number + green "Good rep" cue from the exercise's own success criterion + amber "Progression" gate) → **per-exercise progress chart** → then Why / Setup / Execution / Red flags / Success.

**Smart chart — the metric is exercise-appropriate, not one universal line** (`exChart(ex)`):
- External rotation → loaded-ER **pain** trend (from `assessments.erPain`), lower = better, gate line = current-phase erPain gate.
- Ankle rock → **heel-lift mm** trend (from `assessments.squatHeel`), lower = better, gate line = squatHeel gate.
- Loaded lifts (`load==="kg"`) → **top clean weight** per session (from `exHistory`), higher = better, faint aim line at last+2.5 kg.
- Bodyweight lifts → **total clean reps** per session, higher = better, target line at sets×reps.
Each chart shows the dots it has plus a faint dashed target/gate line to aim at (sparse early — one dot until a few sessions accumulate; pain/mobility charts stay empty until the matching Plan-tab test is logged, and say so).

**Why plain volume was rejected (decision, per Judgment Protocol):** set×rep×load volume is a hypertrophy metric, is *gameable by the very right-side compensation we're correcting*, and is blind to half the program (balance, eccentrics, carries, mobility). Progress is proven by load-at-fixed-clean-reps, a clean extra rep, slower eccentrics/range, falling pain, and the test gates — never tonnage. The extra rep is offered as evidence, never demanded ("form breaks? stop — that's a pass"). Per-exercise graph deliberately kept inside ⓘ, not on the card, to protect minimalism.

## 11. Fakeproof test gates + symmetry (the "don't sprint the gates" decision)
**The challenge (user):** "What if I just game the tests to reach Phase 2 as fast as possible? The exercises are only a tool to pass." **The answer: no — and here's why it's the wrong model.** The test is a *cheap proxy* for the goal (a right side that initiates, stabilizes, shares load), not the goal itself. Gaming the proxy (Goodhart's law) rehearses the exact compensation Phase 1 exists to remove; adaptations are time-bound (tissue/motor-control remodel over weeks), not test-bound; and Phase 2 *loads* what Phase 1 built — so entering on a gamed foundation loads the compensation onto a post-surgical shoulder = re-injury risk. Passing fast isn't winning early, it's failing slowly.

**But the user was half-right:** if the tests are the gate, the tests must be hard to fake. So rather than rely on "be honest," the gates were engineered so the easiest honest way to clear them is to actually do the clean work. Two mechanisms:
1. **Intrinsically hard-to-fake protocols** — balance moved to **eyes-closed** (strips the vision-lock cheat, probes the real proprioceptive deficit); push-ups require a **3s eccentric** + symmetry; walk requires **no next-day flare**; loaded-ER must be at a **recorded working load** (can't pass by sneaking lighter); deep-squat requires **heel-flat + knee-tracking + 3s hold**.
2. **Per-test clean-rep checklist** (`checks:[...]` on every test) — the app will not register a gate as cleared on the number alone. `gateMet()` now passes only if the gate number is met **and** the latest logged entry has `clean===true` (all checks ticked). Trivial gates that the baseline already satisfies (pull-ups P1 = 0) auto-pass without a log.

**New symmetry gate** (`dual:true`): logs **Left + Right** eyes-closed balance, stores `{date,val,L,R,clean}`, `val = round(R/L*100)`. Phase-1 gate = **right ≥ 85% of left**. This is the gate that most directly says "the compensation is gone" and refuses Phase 2 even if every other test passes. (85% = commonly-cited return-to-function heuristic, labelled as such — not a hard law.)

**Assessment schema change:** entries are now `{date,val,clean}` (+`L,R` for dual). `latestEntry()` added; `testCurrent()`/`gateMet()` rewritten. **First logged value of each test = the user's real baseline** (he had not trained at all before building this; he logs all 7 fresh the next morning). Recalibrated Phase-1 targets: balance 15s eyes-closed, symmetry 85%, push-ups 8 @3s, squatHeel ≤15mm, walk 25min, pull-ups 0, erPain ≤2/10. Exact numbers = MODERATE confidence (re-baseline and adjust per-test with a logged reason); the fakeproofing logic = HIGH confidence. TEST_HOW upgraded with the protocols + "Doesn't count if" disqualifiers.

## 12. Corrective primer + calm home (the "expert panel" pass)
**Frame:** user asked for a world-class upgrade as if a panel (Gray Institute, NASM, Jony Ive, et al.) were advising. Honest stance taken: did **not** fabricate quotes or channel people whose frameworks aren't known (Justin Piece, Jason Colleran explicitly left out pending the user describing them); used only documented methods as lenses — **NASM Corrective Exercise Continuum** (inhibit→lengthen→activate→integrate), **Gray AFS** (3D, chain-reaction, active prep), **Apple HIG / Ive** (reduction, one primary action). Verdict given first: the app is already past world-class for its job; the binding constraint is reps not yet done, not features — so most "world-class" ideas (wellness check, autoregulation) were deferred to post-data v-next, and only two root-cause-serving changes shipped now.

**The corrective primer (`PRIMERS`, `primerHTML`, `nearestGate`).** The program already did the back half of the CEx continuum (activate/integrate); the missing front half — **inhibit→lengthen** on the *overactive* tissue, before load — is now a ~2-min, day-specific, 3-move primer framed **FREE → OPEN → WAKE** (Gray's 3D/active style; the session remains the load/integrate). Mapped to the user's chain: **Lower** = free right calf (roller/ball) → 3D ankle glide → wake right glute-med; **Upper** = release right pec (lacrosse ball pinned to a wall, **arm down**) → foam-roller t-spine extension → serratus + lower-trap wake; **Athletic** = calf release → world's greatest stretch → glute-med + dead-bug brace. Launches from the home hero ("Prime · 2 min") and a dashed strip atop the Train tab; opens as an overlay sheet with ephemeral check-offs. **Not tracked** (user's call — no logging utility; protects minimalism).

**Safety:** the Upper FREE move is a **ball release with the arm low**, deliberately chosen over a doorway *stretch* because the user's gear made it possible and it never approaches the apprehension (abduction+ER) position the surgery addressed — a real safety upgrade. Carries an in-sheet ⛔ shoulder-rule flag; physio still clears range. Equipment now on hand: foam roller, lacrosse ball, mat, knee cushion, 4 yoga blocks — primer uses all where useful.

**Confidence labels:** that the primer targets the root cause = HIGH; the *magnitude* of benefit vs. just training well = genuinely UNCERTAIN (stated as such); the specific tight/weak assignments = INFERRED from the user's described pattern, not a hands-on exam → physio confirms, especially the pec work.

**Calm home redesign (`homeHTML` rewritten).** User found the home a busy dashboard ("so much information... I don't know what I should see"). Stripped to one question — *what now?* — per Ive: small greeting+date, two chips (streak, phase), a single dark **hero** (today's session + Prime + Start), one **dynamic nearest-gate nudge** (`nearestGate()` picks the unmet gate closest to clearing), and a slim Fuel line (kept — fat loss is a primary goal with a daily action). **Removed (relocated, not lost):** rotating mission headline (gone), phase ring + "why" card (→ Plan), three stat tiles (→ Progress; a streak chip stays). Pure subtraction — lowest-risk change, since everything cut already exists in another tab.

**Deferred to post-data (not built):** end-of-session wellness check (confidence/pain/symmetry → early-warning line) and light autoregulation (fatigue nudge). Both wait for ~2 weeks of real logs so they're authored from the user's reality, not imagination — consistent with the Foundation Protocol and the standing "don't let app-building become productive procrastination" guard.

## 13. The knee question → a multi-driver model (v10)
**Trigger:** user raised that his **#1 lived concern is right-knee strain** (chair-rise, stairs, walking, pickleball) — front-of-kneecap, achy, **no swelling/locking/giving-way** → reads as patellofemoral-type, the classic *victim-joint* picture. Then he ran two unprompted self-experiments that reshaped the model:

1. **Chair test:** consciously *relaxing the foot/ankle* before standing → glute activates → knee strain drops. This reframed his ankle problem from *structural stiffness* to a **neural guarding pattern** (foot/toes gripping 24/7, "protection mode") — because guarding releases on attention; tissue length doesn't. The locked base switches the glute off and the knee covers for it.
2. **Car + trunk-fold test:** after ~1 hr sitting (foot unloaded/irrelevant) the **right glute** strains; and his boxer waistband **folds only on the right, toward the groin, left intact** → visible sign of a real **right pelvic asymmetry**. He confirmed this seated strain is **worse when shoulder/posture is tired** → top-down (shoulder→scapular drop→trunk→pelvis→glute), a *partially separate* driver from the standing ankle→knee one.

**Honest model update (logged per Judgment Protocol):** Claude had a bottom-up bias (ankle→knee) and under-weighted the shoulder's role; the car + trunk-fold evidence corrected toward a **multi-driver model** — (a) standing knee strain = ankle-guarding driven; (b) seated glute strain = shoulder/posture-fatigue driven. Claude pushed back on collapsing everything into a single shoulder-origin story (two symptoms behave differently → more than one thing).

**What v10 ships (all approved via rendered prototypes first):**
- **Foot Reset** — new lead primer step (`Reset` phase, purple) on **Lower + Athletic**: grip-hard-3s → full release → soft-foot walk. The down-regulation drill the user discovered; carries a "✦ the move you found" note. Down-regulation, *not* stretching (stretching a guarded foot makes it brace harder).
- **Soft-foot in-lift cue** (`footCue`, `FOOT_IDS`) — dark banner on every knee/foot-loaded lift: "Soft foot → feel the right glute → then drive."
- **Daily Knee/Foot/Seat check** (`checkHTML`, `checkChartSVG`, `S.checkins[]`, schema v6) — three 0–10 steppers (knee strain ↓, foot-relax ↑, seated-glute ↓ optional) + a **convergence chart** (knee red vs foot green). The *relationship* is the diagnosis: lines converging = ankle-guarding→knee link confirmed; foot frees but knee doesn't settle = look elsewhere. Built to be **falsifiable on purpose**. Home gets one slim "Today's check" line; not streak-bound.
- **Seated reset** cue (even sit-bones · stack tall through the right · move every ~40 min) — safe, **direction-agnostic**.

**Boundary held (important):** the app **tracks and supports** the pelvis/seated issue but **does not prescribe a pelvic correction** — correcting a rotation/tilt depends on which way it sits; guessing wrong can entrench it. The seated-glute fix is routed through the **shoulder/postural endurance work already in the program**, not a blind pelvic drill. **Agreed tripwire:** if seated strain / trunk fold hasn't eased in **~6–8 weeks** of postural work (or anything worsens), that converts to one **hands-on physio assessment** — user chose app-first; this keeps it tested, not hoped on. Safety reminders kept: achy-no-swelling is workable, but 18 months post-op the pelvic finding warrants an eventual in-person look.

**Process note for next session:** user fairly pointed out Claude should run a *structured* movement screen proactively (loaded vs unloaded, each joint/plane, L/R, seated/standing) rather than discovering drivers by the user volunteering them. **TODO: run that structured intake** before the next programming change.

## 14. Daily-movement guidance + squat-hold animation (v11)
**Trigger:** user flagged the "Daily — every day counts" strip had zero guidance — no how, how much, or what to track, and (unlike the main lifts) no animated motion panel. Fair gap: the most foundational daily work was the least explained.

**Shipped:**
- **Info page per daily movement** (`HABIT_INFO`, `habitHTML`) — Why (goal-tied) / How (numbered) / How much (dose) / What to track. Tracking deliberately minimal: "just check it off; the *measured* version lives in the Plan tests" — no new logging burden. Each daily row now shows name + ⓘ + a dose subline; the ⓘ opens the guide, the check circle still toggles done (the two were split apart). Section gained a one-line intro ("consistency, not max effort…").
- **Animations, scoped honestly** (user chose: animate squat hold + ankle drill, cue-pages for the rest). **Ankle drill reuses the existing `ank_rock` spec** (knee-to-wall = exactly that movement — no new authoring). **Deep squat hold = one new spec `daily_squat`**, authored in the engine's mobility-hold style (`legendLabel:"WHAT OPENS & HOLDS (mobility hold)"`, hold-type muscles: ankles open / hips open / quads hold, no fake firing order — same honesty standard as `ank_rock`/`farmer`/`pallof`). **Balance, crawling, walk = written cue-pages, no animation** — balance is a hold not a path; crawling is contralateral/multi-limb that the 2D side-on engine can't show honestly; walk is locomotion with no meaningful single-rep loop. Claude declined to force decorative animation for these and said so.

**Process note (carried, still open):** the promised **structured movement screen** has not yet been run — user has been driving the diagnostic detail (knee video, leg-raise tests). After his self-tests tonight (foot guarding, dorsiflexion offloads the knee, posterior neural/hamstring tightness on right SLR with a clean neuro screen — stretch only, no numbness/tingling/weakness), Claude advised: stop self-provoking the knee, **get one in-person physio assessment** (now past "cheap insurance" given preoccupation), and start training — the knee improves by being used well, not watched. The convergence chart (knee↓/foot↑) is the in-app proof loop. **TODO unchanged: run the structured intake before the next programming change.**

**Wellbeing note:** watch for over-monitoring of the knee feeding anxiety; Claude redirected toward action (train + assess) rather than more self-testing, and held the "app supports, doesn't diagnose; physio for hands-on" boundary throughout (esp. once neural tension appeared).

